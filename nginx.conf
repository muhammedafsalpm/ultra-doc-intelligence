http {
    client_max_body_size 100M;
    
    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}

events {
    worker_connections 1024;
}
